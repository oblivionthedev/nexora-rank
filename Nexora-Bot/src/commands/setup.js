import { PermissionFlagsBits, SlashCommandBuilder } from "discord.js";
import { colors, embed } from "../lib/response.js";
export const setupCommand={data:new SlashCommandBuilder().setName("setup").setDescription("Show the Nexora server setup checklist").setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).setDMPermission(false),async execute(interaction,{config}){const response=embed("Set up Nexora","Complete these steps in order. Your plan-specific link code expires after ten minutes.",colors.roseGold).addFields({name:"1 · Open your dashboard",value:`[Open Nexora](${config.siteUrl}/dashboard)`},{name:"2 · Choose Connections",value:"Create a link code matched to the workspace plan."},{name:"3 · Link this server",value:"Run `/link code:YOUR_CODE` here."},{name:"4 · Verify",value:"Run `/diagnostics` and confirm every required check passes."});await interaction.editReply({embeds:[response]})}};
