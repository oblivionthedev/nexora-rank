import { PermissionsBitField, SlashCommandBuilder } from "discord.js";
import { colors, embed } from "../lib/response.js";
export const diagnosticsCommand={data:new SlashCommandBuilder().setName("diagnostics").setDescription("Check the Nexora connection and bot permissions").setDMPermission(false),async execute(interaction,{nexora}){const summary=await nexora.workspaceSummary(interaction.guildId,interaction.user.id);const me=interaction.guild.members.me;const required=[PermissionsBitField.Flags.ViewChannel,PermissionsBitField.Flags.SendMessages,PermissionsBitField.Flags.EmbedLinks,PermissionsBitField.Flags.ManageRoles];const missing=required.filter(p=>!me?.permissions.has(p));const ok=summary.workspace.operational_status==="active"&&missing.length===0;const response=embed(ok?"Nexora is ready":"Nexora needs attention",ok?"The workspace connection and required bot permissions are available.":"Review the checks below before using live operations.",ok?colors.success:colors.warning).addFields({name:"Workspace",value:summary.workspace.operational_status==="active"?"✅ Operational":"❌ Restricted",inline:true},{name:"Discord link",value:summary.workspace.discord_guild_id===interaction.guildId?"✅ Connected":"❌ Incorrect server",inline:true},{name:"Bot permissions",value:missing.length?`❌ ${missing.length} required permission(s) missing`:"✅ Ready",inline:true},{name:"Roblox execution",value:"⏸️ Deferred until the approved connection is enabled"});await interaction.editReply({embeds:[response]})}};
