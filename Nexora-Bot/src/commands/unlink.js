import { PermissionFlagsBits, SlashCommandBuilder } from "discord.js";
import { colors, embed } from "../lib/response.js";
export const unlinkCommand={data:new SlashCommandBuilder().setName("unlink").setDescription("Disconnect this Discord server from Nexora").addStringOption(o=>o.setName("confirm").setDescription("Type DISCONNECT to confirm").setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).setDMPermission(false),async execute(interaction,{nexora}){if(interaction.options.getString("confirm")!=="DISCONNECT")return interaction.editReply({embeds:[embed("Confirmation required","Run the command again and type **DISCONNECT** exactly.",colors.warning)]});await nexora.disconnectGuild(interaction.guildId,interaction.user.id);await interaction.editReply({embeds:[embed("Server disconnected","This Discord server can no longer use workspace commands. Reconnect it from the Nexora dashboard.",colors.warning)]})}};
