import { ShieldCheck, UserRound, UsersRound } from "lucide-react";
import { PageHeading } from "@/components/workspace-shell";
import { getWorkspaceControl } from "@/lib/workspace-control";
import { inviteMember, manageMember } from "../actions";
import { Input, Select, Submit } from "@/components/workspace-operations";

export const dynamic="force-dynamic";
export default async function MembersPage({params,searchParams}:{params:Promise<{workspaceId:string}>;searchParams:Promise<{saved?:string;error?:string}>}){const[{workspaceId},q]=await Promise.all([params,searchParams]);const{supabase,state}=await getWorkspaceControl(workspaceId);const{data:members}=await supabase.from("workspace_members").select("user_id, role, joined_at").eq("workspace_id",state.workspace.id).order("joined_at");const ids=(members||[]).map(m=>m.user_id);const{data:profiles}=ids.length?await supabase.from("profiles").select("id, display_name, contact_email").in("id",ids):{data:[]};const profileMap=new Map((profiles||[]).map(p=>[p.id,p]));const manage=["owner","admin"].includes(state.workspace.role);return <><PageHeading eyebrow="Members" title="Workspace team" description="Invite collaborators, assign only the access they need, and remove access immediately."/>{q.saved?<p className="mt-6 rounded-2xl border border-emerald-300/20 bg-emerald-300/8 p-4 text-sm text-emerald-100">Team updated.</p>:null}{manage?<form action={inviteMember} className="mt-8 grid gap-4 rounded-[28px] border border-white/10 bg-white/[.025] p-6 sm:grid-cols-[1fr_180px_auto] sm:items-end"><input type="hidden" name="public_id" value={workspaceId}/><Input label="Member email" name="email" type="email" required/><Select label="Role" name="role"><option value="viewer">Viewer</option><option value="reviewer">Reviewer</option><option value="operator">Operator</option><option value="admin">Admin</option></Select><Submit>Invite member</Submit></form>:null}<section className="mt-5 rounded-[28px] border border-white/10 bg-white/[.025] p-5 sm:p-7"><div className="flex items-center gap-3 border-b border-white/8 pb-5"><UsersRound className="workspace-accent size-5"/><div><h2 className="text-xl font-bold">{members?.length||0} members</h2><p className="mt-1 text-sm text-white/40">Owner, admin, operator, reviewer, and viewer roles</p></div></div><div className="divide-y divide-white/7">{(members||[]).map(member=>{const p=profileMap.get(member.user_id);return <div key={member.user_id} className="flex flex-wrap items-center gap-4 py-5"><span className="flex size-11 items-center justify-center rounded-full bg-white/6"><UserRound className="size-5 text-white/50"/></span><div className="min-w-0 flex-1"><p className="truncate text-base font-bold">{p?.display_name||"Nexora member"}</p><p className="mt-1 truncate text-sm text-white/38">{p?.contact_email||"No contact email"}</p></div>{manage&&member.role!=="owner"?<form action={manageMember} className="flex items-center gap-2"><input type="hidden" name="public_id" value={workspaceId}/><input type="hidden" name="user_id" value={member.user_id}/><select name="role" defaultValue={member.role} className="min-h-10 rounded-lg border border-white/10 bg-[#0b0808] px-3 text-sm"><option>viewer</option><option>reviewer</option><option>operator</option><option>admin</option></select><button name="action" value="role" className="text-sm font-bold">Save</button><button name="action" value="remove" className="text-sm text-red-200/70">Remove</button></form>:<span className="workspace-accent-surface flex items-center gap-2 rounded-full border px-3 py-1.5 text-xs font-bold capitalize"><ShieldCheck className="size-3.5"/>{member.role}</span>}</div>})}</div></section></>}
