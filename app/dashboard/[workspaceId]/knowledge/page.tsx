import { BookOpen, NotebookPen } from "lucide-react";
import { PageHeading } from "@/components/workspace-shell";
import { Empty, Input, Notice, Panel, Row, Select, Submit, Textarea } from "@/components/workspace-operations";
import { getWorkspaceControl } from "@/lib/workspace-control";
import { createKnowledgeEntry, deleteRecord } from "../actions";
export const dynamic="force-dynamic";
export default async function Knowledge({params,searchParams}:{params:Promise<{workspaceId:string}>;searchParams:Promise<{saved?:string;error?:string}>}){const[{workspaceId},q]=await Promise.all([params,searchParams]);const{supabase,state}=await getWorkspaceControl(workspaceId);const{data:entries}=await supabase.from("knowledge_entries").select("*").eq("workspace_id",state.workspace.id).order("updated_at",{ascending:false});return <><PageHeading eyebrow="Knowledge" title="Knowledge base and logbook" description="Keep procedures, resources, operational notes, and logbook entries available to your staff."/><Notice {...q}/><div className="mt-8 grid gap-5 xl:grid-cols-[.9fr_1.1fr]"><Panel icon={NotebookPen} title="New entry" description="Choose who can see it and what kind of record it is."><form action={createKnowledgeEntry} className="grid gap-4"><input type="hidden" name="public_id" value={workspaceId}/><Input label="Title" name="title" required/><div className="grid gap-4 sm:grid-cols-2"><Select label="Type" name="entry_type"><option value="article">Article</option><option value="procedure">Procedure</option><option value="resource">Resource</option><option value="logbook">Logbook</option></Select><Select label="Visibility" name="visibility"><option value="staff">Staff</option><option value="members">Members</option><option value="public">Public</option></Select></div><Textarea label="Content" name="content" required/><div><Submit>Publish entry</Submit></div></form></Panel><Panel icon={BookOpen} title="Entries" description="Newest changes appear first.">{entries?.length?entries.map(x=><Row key={x.id} title={x.title} subtitle={x.content.slice(0,140)} meta={`${x.entry_type} · ${x.visibility}`} action={<form action={deleteRecord}><input type="hidden" name="public_id" value={workspaceId}/><input type="hidden" name="path" value="knowledge"/><input type="hidden" name="table" value="knowledge_entries"/><input type="hidden" name="id" value={x.id}/><button className="text-sm text-red-200/70">Remove</button></form>}/>):<Empty>No knowledge entries yet.</Empty>}</Panel></div></>}
